DATAS SEGMENT
    ;此处输入数据段代码  
DATAS ENDS

STACKS SEGMENT
    ;此处输入堆栈段代码
STACKS ENDS

CODES SEGMENT
    ASSUME CS:CODES,DS:DATAS,SS:STACKS
START:
    MOV AX,DATAS
    MOV DS,AX
    
    MOV CX,0
    
LOOP1:

	MOV AH,01
	INT 21H
	CMP AL,0DH
	JZ  EXIT0
	SUB AL,'0'
	CMP AL,10
	JNL NEXT1
	CBW
	ADD CX,AX

NEXT1:
	
	JMP LOOP1

EXIT0:
	
	MOV AH,02H
	ADD CL,'0'
	MOV DL,CL
	INT 21H
	MOV AX,CX
    ;此处输入代码段代码
    MOV AH,4CH
    INT 21H
CODES ENDS
    END START


